#!/usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail

# Nothing has to be bootstrapped here: CMake >= 3.20 is assumed to be available
# and every third-party dependency is fetched by CPM.cmake during configure.
mkdir -p build
